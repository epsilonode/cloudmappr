# cloudmappr

@roadmap cloudmappr
@log ../logs/cloudmappr.logfmt
@meta name=roadmap-format content="../format-guidance.md"
@meta name=memory-format content="../making-memories.md"
@meta name=ledger-router content="../control.yaml"
@meta name=discovery-guidance content="../discovery-guidance.md"
@meta name=roadmap-status content="active"
@updated 2026-08-20 @summary
Cloudmappr is an isomorphic world-map product: a browser MJS editor/component and
a Deno rendering API use one declarative map specification and the same released
world data to deliver interactive SVG maps and canonical shareable images.

## @tier8 active baseline

### @work @active isomorphic mjs d3 world map

@scope baseline product runtime @target Deliver the first Cloudmappr product as
a framework-neutral MJS D3 world map with static TopoJSON shards and a matching
server-rendered image API.

@memory ../memories/cloudmappr/strategy/isomorphic-baseline-implementation-plan.md
@memory ../memories/cloudmappr/contracts/map-spec-and-scene.md
@memory ../memories/cloudmappr/contracts/overlay-projection-parity.md
@memory ../memories/cloudmappr/strategy/world-topojson-shards.md
@memory ../memories/cloudmappr/strategy/github-esm-artifact-delivery.md
@memory ../memories/cloudmappr/strategy/map-component-migration-completeness.md

- [x] @accept export a framework-neutral `createMap(host, options)` MJS entry;
      map coverage is global and is not tied to Corfeed or any UI framework
      @evidence 2026-08-20: `packages/client/src/create-map.ts` provides the
      framework-neutral controller with keyed land/marker/label joins and teardown
- [x] @accept use D3 selection and `d3-geo` for SVG map rendering and use
      `topojson-client` to decode released TopoJSON shards before rendering
      @evidence 2026-08-20: shared `map.ts` uses `d3-geo`; browser joins use
      `d3-selection`; `topologyLandDecoder` and live HTTP scene proof decode shards
- [ ] @accept load one eager minor-island basemap plus seven lazy regional-land
      shards selected by viewport bounds and cached by immutable artifact URL
- [x] @accept define one versioned `MapSpec` that drives browser rendering,
      server SVG/PNG rendering, image sharing, and future framework adapters
      @evidence 2026-08-20: `packages/core/src/map.ts` is consumed by browser and
      Deno render composition
- [x] @accept require a geographic bounding box in the first renderable spec and
      reserve validated points, labels, categories, and colors in the same model
      @evidence 2026-08-20: bounded `MapSpec` validation has atomic proof
- [x] @accept use one projection, logical viewBox, antimeridian policy, map-data
      release, and scene order for all client/server rendering paths
      @evidence 2026-08-20: shared Mercator scene builder and fixture HTTP/filesystem
      live proofs use the same bounds selection and SVG scene
- [ ] @accept keep pre-rendered SVG land paths out of this baseline until their
      upstream conversion and comparative delivery proof is accepted

### @work @active canonical cloud image rendering

@scope shareable map output @target A user can compose a map in the client and
receive an immutable cloud-rendered image link for media, messaging, and embeds.

@memory ../memories/cloudmappr/contracts/canonical-render-api.md
@memory ../memories/cloudmappr/strategy/client-export-and-sharing.md
@memory ../memories/cloudmappr/contracts/overlay-projection-parity.md

- [x] @accept `POST /v1/renders` accepts a validated `MapSpec`, canonicalizes it,
      and returns content-addressed canonical SVG and PNG URLs
      @evidence 2026-08-20: live API proof stores and returns SHA-256-addressed outputs
- [x] @accept `GET /v1/renders/:id.svg` and `GET /v1/renders/:id.png` return
      immutable output rendered from the declared spec and artifact release
      @evidence 2026-08-20: immutable cache headers are asserted in render API proof
- [x] @accept PNG is a hard server-side requirement; SVG is the canonical vector
      representation; server WebP is optional until a compatible encoder is proven
      @evidence 2026-08-20: `@resvg/resvg-js` produces PNG in the server render path
- [x] @accept a shared map URL can display the PNG without downloading or running
      the MJS component
      @evidence 2026-08-20: GET PNG uses stored server-generated bytes only
- [x] @accept Deno renders server output with the same scene construction and
      projection contract as the browser, not from arbitrary client SVG
      @evidence 2026-08-20: browser and Deno loaders are proven against one fixture release
- [x] @accept render identity includes canonical spec, output dimensions, theme,
      renderer version, fonts/styles, and released world-data manifest version
      @evidence 2026-08-20: `renderIdentity` includes canonical spec, release,
      renderer/style/projection versions

## @tier6 active contracts

### @contract @active map spec and scene model

@scope browser/server interchange @target Map composition is declarative, safe,
versioned, and sufficient for client editing as well as server rendering.

@memory ../memories/cloudmappr/contracts/map-spec-and-scene.md
@memory ../memories/cloudmappr/contracts/overlay-projection-parity.md
@memory ../memories/cloudmappr/contracts/label-placement-and-visibility.md

- [x] @accept `MapSpec` has a format version, geographic bounds, output size,
      theme/style selection, and optional point and label layer arrays
      @evidence 2026-08-20: `packages/core/src/map.ts` validates the versioned model
- [x] @accept points and labels use stable IDs, longitude/latitude, escaped text,
      explicit priority, and bounded category/color values
      @evidence 2026-08-20: shared validator and SVG serializer are atomically proven
- [x] @accept client/server share `validateMapSpec`, `canonicalizeMapSpec`,
      `buildMapScene`, and SVG serialization contracts from browser-safe code
      @evidence 2026-08-20: browser and Deno packages import `packages/core/mod.ts`
- [ ] @accept scene layers render in deterministic order: land, markers, labels,
      and controls/overlays
- [x] @accept the API rejects malformed bounds, excessive output dimensions,
      unbounded layer counts, arbitrary CSS, scripts, and arbitrary SVG payloads
      @evidence 2026-08-20: `map.test.ts` proves input bounds and unsafe-field rejection

### @contract @active world artifact manifest

@scope pan-first world land delivery @target The accepted v6 release composes
the initial viewport from one wrapped set of immutable artifacts, then uses
reproducible lazy selection only for later pans; closed optional artifacts remain
measured additions to that release.

@memory ../memories/cloudmappr/strategy/world-topojson-shards.md
@memory ../memories/cloudmappr/strategy/seeded-geographic-shard-partition.md
@memory ../memories/cloudmappr/strategy/rule-first-geographic-exceptions.md
@memory ../memories/cloudmappr/decisions/ten-metre-policy-and-emission.md
@memory ../memories/cloudmappr/strategy/world-artifact-generation-toolchain.md
@memory ../memories/cloudmappr/contracts/geographic-partition-functional-core.md
@memory ../memories/cloudmappr/contracts/artifact-receiver.md
@memory ../memories/cloudmappr/contracts/initial-artifact-payload-delivery.md
@memory ../memories/cloudmappr/strategy/local-artifact-development.md
@memory ../memories/cloudmappr/strategy/github-esm-artifact-delivery.md
@memory ../memories/cloudmappr/contracts/world-artifact-provenance.md

- [x] @accept generate `world-basemap`, `north-america`, `south-america`,
      `europe`, `africa`, `asia`, `oceania-major`, and `antarctica` as
      independent TopoJSON files
      @evidence 2026-08-20: 10m live generation dissolves and independently
      decodes all eight artifacts in `world-atlas.live.test.ts`
- [ ] @accept use the Natural Earth / World Atlas 10m source for the released
      coastline; regenerate review material rather than approving the coarse 110m
      candidate table
- [ ] @accept every atomic source land part appears in exactly one active
      artifact; `world-basemap` contains only residual land not assigned by a
      major-region or accepted successor policy, including Micronesia until a
      measured Pacific-island successor is promoted
- [ ] @accept the manifest declares artifact ID, immutable URL, geographic
      coverage, object name, eager/lazy state, and world-data release identity;
      the accepted v6 pan-first release uses antimeridian-safe `coverageBounds[]`
      for optional artifacts and may wrap the initial viewport's selected shards
      in one payload without changing their immutable artifact identities
- [ ] @accept a Deno Deploy initial-payload boundary derives one canonical
      release-plus-selected-ID key through the shared selector, serves a framed
      digest-verified pack with negotiated outer Brotli/gzip compression and
      immutable edge-cache headers, and seeds only validated per-artifact browser
      cache entries before later pans lazily fetch absent IDs
      @evidence 2026-08-20: transport-free format-1 framing, selection-key,
      manifest-carrying index, and per-member verification primitives are proven;
      Deploy/browser adapters remain open
- [ ] @accept client and server select the same shards for the same normalized
      bounds, including bounds crossing the antimeridian
- [ ] @accept viewport panning is never clamped by shard availability; selection
      uses current bounds plus documented prefetch/retention policy
- [ ] @accept evaluate the proposed seven-seed, geodesic-frontier, hard-corridor
      partition policy against 10m release measurements before approving its
      country/territory policy and polygon overrides
- [x] @accept materialize a total, deterministic, explained assignment from
      geographic rules and seed paths; explicit atomic-part exceptions are
      source-release-scoped, justified, and used only when no rule can express
      the intended whole-part result
      @evidence 2026-08-20: `partition.ts`, the checked-in seeded-frontier policy,
      and 10m policy-report test prove all 4,220 extracted parts are explained
- [ ] @accept the build-only Mapshaper adapter and Deno TypeScript generator each
      have a contract; pure partition, validation, and manifest primitives never
      import filesystem, process, network, or CLI effects
- [ ] @accept the artifact receiver plans and reduces immutable state in pure
      code; browser HTTP, Deno filesystem, decoding, digests, and promise caches
      are injected effect adapters
- [x] @accept pure receiver planning selects eager and viewport-intersecting
      shards in manifest order, handles antimeridian bounds, and rejects duplicate
      received shards without performing I/O
      @evidence 2026-08-19: `packages/world-artifacts/src/receiver.ts` and four
      colocated atomic tests pass through `mise run verify`
- [x] @accept raw manifest values admit the required eight core descriptors and
      one eager `world-basemap`; v1 normalizes its one `bounds` rectangle while
      v2 additionally permits each named optional artifact at most once through
      a non-empty antimeridian-safe `coverageBounds[]` list
      @evidence 2026-08-20: `parseManifest` rejects incomplete core, duplicate,
      unknown, malformed-coverage, and traversal declarations; receiver tests
      prove v1 normalization and v2 optional Pacific-footprint selection
- [x] @accept the receiver interpreter loads only planner commands through
      injected manifest-byte, manifest-decoder, artifact-byte, digest, and
      topology-decoder ports; a digest mismatch prevents decode and state reduction
      @evidence 2026-08-19: two fake-port seam tests in
      `receiver-runtime.seam.test.ts` pass through `mise run verify`; raw manifest
      values are parsed before `reduceManifest` is reached
- [x] @accept local development serves one configured release directory through
      manifest-listed URLs only, and browser and Deno receivers select identical
      artifacts for every normalized bounds fixture
      @evidence 2026-08-20: local route, browser HTTP loader, filesystem renderer,
      and canonical API are proven with generated fixture release live tests

### @work @ready 10m candidate promotion and measurement

@scope local generated 10m release @target Produce a reproducible evidence
package for policy approval and immutable delivery handoff without publishing or
changing the eight-shard baseline.

@memory ../memories/cloudmappr/strategy/ten-metre-candidate-promotion.md
@memory ../memories/cloudmappr/contracts/world-artifact-provenance.md
@memory ../memories/cloudmappr/contracts/artifact-receiver.md

- [ ] @accept write a canonical policy audit report that groups every 10m part
      by shard, explanation, rule, source territory, corridor proximity,
      antimeridian status, and residual fallback; reject stale or unexplained
      assignments before release generation
- [ ] @accept generate a digest- and policy-revision-named candidate directory
      containing only the eight TopoJSON artifacts, manifest, and provenance;
      re-read it to prove exact coverage, artifact digest integrity, independent
      decoding, and absence of temporary source/intermediate files
- [ ] @accept prove browser HTTP and Deno filesystem receivers select identical
      artifact IDs from the accepted v6 release for regional, boundary, polar,
      and antimeridian bounds; initial selection may arrive as one wrapped
      payload and later pan-induced acquisitions remain lazy
- [x] @accept emit a canonical measurement record for raw/gzip/Brotli bytes,
      request count, decode/path time, and peak retained memory with median/range
      across North America, Europe/Africa, Pacific, Arctic, Southern Ocean,
      antimeridian, and full-world scenarios
      @evidence 2026-08-20: `artifacts:measure` generated three-run v3–v6
      records under `build/measurements/`; v6 is accepted for pan-first delivery,
      while full-world numbers remain recorded compatibility evidence
- [ ] @accept require a review record that compares the v3 policy/explanation
      diff and measurements against v2 before handing the immutable directory to
      GitHub/esm.sh publication

### @contract @active browser composition and export

@scope browser component use cases @target The editor is not the sole product;
plain map components work in any UI framework and may export/copy images.

@memory ../memories/cloudmappr/strategy/client-export-and-sharing.md
@memory ../memories/cloudmappr/contracts/overlay-projection-parity.md
@memory ../memories/cloudmappr/contracts/custom-element-wrapper.md
@memory ../memories/cloudmappr/contracts/map-presentation-and-accessibility.md
@memory ../memories/cloudmappr/contracts/label-placement-and-visibility.md
@memory ../memories/cloudmappr/contracts/svelte-wrapper-boundary.md

- [ ] @accept consumers can set bounds, points, labels, categories, and colors
      through MJS controller inputs without importing business/domain code
- [ ] @accept the component supports local SVG export and browser PNG export
      with dimension limits, plus optional WebP with PNG fallback
- [ ] @accept copy-to-clipboard runs only with a user gesture and HTTPS-capable
      Clipboard APIs, with download as a reliable fallback
- [ ] @accept callers can request the cloud canonical render and use its PNG URL
      for identical shared output rather than trusting local canvas rasterization
- [ ] @accept thin adapters for UI frameworks or a Custom Element are optional;
      they wrap the MJS controller and do not own rendering logic
- [ ] @accept a Svelte wrapper remains an optional consumer-layer adapter over
      the public MJS controller; it does not add SvelteKit or move map behavior
      into components, stores, or routes

### @contract @active component quality and provenance

@scope framework-neutral integration, accessibility, source lineage, and label
behavior @target Preserve generic map quality requirements without importing
Corfeed application behavior or creating a second rendering implementation.

@memory ../memories/cloudmappr/strategy/map-component-migration-completeness.md
@memory ../memories/cloudmappr/contracts/custom-element-wrapper.md
@memory ../memories/cloudmappr/contracts/map-presentation-and-accessibility.md
@memory ../memories/cloudmappr/contracts/world-artifact-provenance.md
@memory ../memories/cloudmappr/contracts/label-placement-and-visibility.md
@memory ../memories/cloudmappr/contracts/overlay-projection-parity.md

- [ ] @accept an optional `<cloudmappr-map>` is a thin MJS-controller wrapper
      with connection-safe property updates, idempotent registration, teardown,
      Shadow DOM isolation, and bubbling composed point events
- [ ] @accept CSS custom properties, host sizing, zero-size startup, resize,
      light/dark defaults, focus indication, and reduced-motion behavior preserve
      presentation without mutating the document-global theme
- [ ] @accept the SVG has an accessible name; only actionable markers are
      keyboard focusable; land and labels are not tab stops; pointer and marker
      interactions remain distinct
- [ ] @accept each world-data release records source/license/version/checksum,
      generator/tool versions, partition rationale, and immutable artifact
      provenance; regeneration tooling remains outside runtime dependencies
- [ ] @accept labels retain stable IDs, priority, and deterministic initial
      offsets; polygon anchor generation and collision layout remain separate;
      suppressed labels never hide their markers

## @tier4 implementation and proof

### @work @ready phased implementation

@scope delivery order @target Build reusable scene logic before deployment/UI
adapters so browser and server behavior cannot drift.

@memory ../memories/cloudmappr/strategy/isomorphic-baseline-implementation-plan.md
@memory ../memories/cloudmappr/strategy/map-component-migration-completeness.md
@memory ../memories/cloudmappr/strategy/runtime-and-functional-baseline.md
@memory ../memories/cloudmappr/strategy/world-artifact-generation-toolchain.md

- [ ] @accept define public types, projection/viewBox policy, and fixture
      manifest before extracting any page-bound map behavior
- [ ] @accept implement shared spec validation, canonicalization, scene building,
      TopoJSON decoding, and D3 path generation before the MJS wrapper
- [ ] @accept implement the eight-shard generator and exact-coverage proof before
      relying on lazy viewport selection
- [ ] @accept pin and invoke Mapshaper only as a build-time transformation adapter;
      the Deno generator owns typed assignment, validation, manifest, and release
      records through functional-core/effect-boundary contracts
- [x] @accept prove the pinned Mapshaper adapter emits eight independent fixture
      TopoJSON artifacts with typed assignment validation, manifest digests, and
      one eager basemap
      @evidence 2026-08-18: `deno task artifacts:prove` passes the explicit live
      fixture proof; `mise run verify` passes the pure primitive tests and normal
      checks without executing build effects
- [ ] @accept implement Deno SVG then PNG rendering from the same shared scene
      before exposing render URLs to the browser
- [ ] @accept add GitHub/esm.sh release delivery and a clean external consumer
      fixture before framework adapters or clipboard conveniences
- [x] @accept bootstrap the declared Deno runtime and record the functional,
      linting, and colocated-test baseline before creating source workspaces
      @evidence 2026-08-18: root `mise.toml` declares latest Deno; `mise run
      verify` passes Deno lint, functional ESLint, Deno type checking, and four
      colocated atomic/seam tests; `mise run test-live` passes the explicit live
      test convention
- [x] @accept migrate the generic Custom Element, presentation, accessibility,
      provenance, label, and deferred SVG-experiment planning contracts identified
      in `map-component-migration-completeness.md`
      @evidence 2026-08-18: component-quality-and-provenance now routes the four
      governing contracts; the deferred experiment remains governed by its own
      retained proof card

### @decision @ready local GitHub Pages build delivery

@scope static demo publication @target Build and verify the Pages site locally,
then publish generated output through a dedicated branch without a repository
GitHub Actions workflow.

@memory ../memories/cloudmappr/strategy/local-github-pages-delivery.md

- [ ] @accept one local command builds the static Pages payload and a second
      local command previews that exact payload before publication
- [ ] @accept publication updates only the root of a dedicated `gh-pages` branch;
      the source branch remains free of generated Pages output
- [ ] @accept the Pages decision does not prescribe the MJS controller, manifest,
      data-artifact build, or release delivery pipeline while those remain open

### @decision @ready esm.sh selected-shard delivery

@scope public module CDN @target Deliver the controller and only manifest-selected
world shards from one immutable GitHub release without eager global geometry.

@memory ../memories/cloudmappr/strategy/github-esm-artifact-delivery.md
@memory ../memories/cloudmappr/strategy/world-topojson-shards.md

- [ ] @accept publish explicit controller, manifest, and per-shard module entry
      points; no public barrel entry may statically import every world shard
- [ ] @accept the manifest maps each selected artifact to one immutable URL and
      browser and Deno resolve the same release identity
- [ ] @accept a release fixture proves esm.sh dynamic imports transfer only the
      eager basemap and viewport-selected shards, with no mutable-ref fallback
- [ ] @accept record and justify any esm.sh transform options, including target,
      bundling, externals, or raw-data delivery, in the delivery manifest

### @proof @ready parity and delivery evidence

@scope baseline acceptance @target Prove semantic and visual parity across the
editor, plain component use, server render service, and shared image URL.

@memory ../memories/cloudmappr/strategy/isomorphic-baseline-implementation-plan.md
@memory ../memories/cloudmappr/contracts/canonical-render-api.md

- [ ] @accept unit tests cover spec canonicalization, bbox normalization,
      antimeridian splits, manifest selection, hash identity, and cache de-duplication
- [ ] @accept DOM tests cover basemap/lazy shard mount, point/label updates,
      keyboard markers, resize, focus, and controller teardown
- [ ] @accept artifact tests prove no atomic land part is duplicated or omitted
      across eight shards and each shard decodes independently
- [ ] @accept client and Deno fixtures produce the same scene geometry, marker,
      label, style, and world-data release selection for identical `MapSpec` input
- [ ] @accept canonical PNG URLs are stable cacheable image responses and render
      correctly when opened without JavaScript
- [ ] @accept client export tests verify SVG, PNG, WebP fallback, clipboard
      fallback, and canvas-safe assets

## @tier2 deferred experiments

### @decision @deferred ocean and polar island shard expansion

@scope residual 10m land delivery @target Accept the v6 coastal-attachment
release as the pan-first successor. Deliver an initial viewport's relevant
shards in one wrapped payload, then fetch only pan-induced selections lazily;
full-world cost is recorded but is not a promotion veto. Next remeasure detached
residuals, prove bounded footprint compaction, trial Indian, and only then
conditionally evaluate Atlantic; do not reopen Arctic or Southern Ocean variants
without new evidence.

@memory ../memories/cloudmappr/strategy/ocean-and-polar-island-shards.md

- [x] @accept assign main Antarctica source part `010:0` to the existing
      `antarctica` artifact before classifying southern residual islands
      @evidence 2026-08-20: `seeded-frontier-v2-antarctica` policy names the
      `antarctica-mainland` whole-part rule; policy and live emission proofs pass
- [x] @accept assign all residual 10m land south of 50 S to the existing lazy
      `antarctica` artifact without adding a ninth manifest descriptor
      @evidence 2026-08-20: `seeded-frontier-v3-southern-ocean` policy/report
      and live emission proof reduce `world-basemap` to 1,942,319 raw bytes
- [x] @accept classify residual atomic land before dissolve into optional lazy
      `islands-arctic`, `islands-southern-ocean`, `islands-epac`, and
      `islands-wpac` artifacts through a second, pure, ordered residual phase;
      retain one-and-only-one explained coverage and prefer named geographic
      rules over atomic-part overrides
      @evidence 2026-08-20: v4/v5 policies and `partition.ts` keep major-region
      assignments first, then classify only residual parts with policy footprints
      and a 350 km major-land detachment budget; no atomic exceptions are used
- [x] @accept evolve manifest and receiver selection from one descriptor bbox to
      a v1-compatible, antimeridian-normalized `coverageBounds[]` model shared by
      browser and Deno; retain a closed core-plus-optional artifact catalog and
      do not split a whole source part as a convenience
      @evidence 2026-08-20: parser normalizes v1 bounds and validates a closed
      optional catalog; v5 live proof establishes browser HTTP/Deno filesystem
      selection parity for a generated multi-footprint release
- [x] @accept trial optional artifacts in evidence-gated order: first
      `islands-arctic`, then the `islands-epac`/`islands-wpac` pair; retain
      Southern Ocean land in `antarctica` and defer Atlantic, Indian, or a
      separate Southern Ocean artifact until a new residual report demonstrates
      a distinct ordinary-view benefit
      @evidence 2026-08-20: generated local v4 Arctic and v5 Arctic/Pacific
      candidate directories plus three-run measurement and v3 comparison records
      reside under `build/world/` and `build/measurements/`
- [x] @accept preserve typed `SourceFeatureId` provenance and emit a pure,
      read-only coastal-attachment audit for every v5 residual part, reporting
      attachable, no-owner, corridor-blocked, ambiguous, and too-distant outcomes
      without mutating assignments, manifests, or emitted artifacts
      @evidence 2026-08-20: `artifacts:world:coastal-audit` writes 1,204
      decisions at 350 km; 519 are attachable, 5 corridor-blocked, 374 have no
      same-territory major owner, and 306 are too distant
- [x] @accept implement a pure, opt-in coastal-attachment phase between
      major-region assignment and detached-residual classification: preserve
      typed source-feature provenance; attach only to a unique same-territory
      major shard within a versioned, source-release-scoped distance budget;
      retain ties, corridor violations, distant, and cross-territory parts in the
      residual basemap with explicit explanations and no ordinary override list
      @evidence 2026-08-20: accepted v6 policy
      `same-territory-coastal-attachment-350km` produces 519 attachments; the
      pure partition test proves attachment precedes residual rules
- [x] @accept emit a post-attachment report that accounts for all 4,220 parts,
      compares the v5 residual baseline of 1,204 parts / 441,055 raw eager bytes,
      and separately reports Indian-window, Atlantic-window, north of 50 N, and
      other diagnostic groups without treating those planning windows as final
      artifact bounds; make this report the sole input to the next
      detached-residual candidate
      @evidence 2026-08-20: v6 report records 1,204 before / 519 attached / 924
      residual parts, a 369,007-byte eager basemap, and residual diagnostics of
      Indian 451, Atlantic 293, north 128, other 52
- [x] @accept adopt the v6 pan-first delivery policy: the initial transport may
      wrap all artifacts relevant to the viewport while retaining their immutable
      identities/digests, and only newly selected artifacts fetch on subsequent
      pans; record full-world measurements but do not use them as a promotion veto
      @evidence 2026-08-20: v6 reduces ordinary North America, Europe/Africa, and
      Pacific raw selections versus v5; the accepted product has no global-map
      view and uses no material zoom workflow
- [ ] @accept permit pure optional-footprint compaction only for overlapping or
      adjacent normalized footprints under a versioned expansion budget; prove
      complete assigned-part coverage, preserved dateline wholes, smaller manifest
      coverage data, and no unrelated ordinary-viewport artifact selection before
      treating compaction as release behavior or evaluating another optional shard
- [ ] @accept after approved attachment and compaction evidence, trial
      `islands-indian` before conditionally evaluating `islands-atlantic`,
      measuring each against v3 and its immediate predecessor for North America,
      Europe/Africa, Indian Ocean, Atlantic, Arctic, Pacific, Southern Ocean,
      antimeridian, and full-world selection, transfer, decode, path, and
      retained-memory costs
- [ ] @accept keep southern land in `antarctica` and avoid a further Arctic split
      unless the post-attachment report and comparative measurement establish a
      distinct detached-land benefit
- [ ] @accept promote later residual expansions only when measured ordinary
      pan-view transfer, decode/path work, retained memory, cache behavior, and
      pan-induced request count improve without antimeridian/polar regressions;
      record full-world composition for compatibility only

### @work @deferred pre-rendered svg land paths

@scope alternative geometry delivery @target Evaluate pre-rendered SVG land only
after the TopoJSON/D3 baseline has delivery, export, and parity evidence.

@memory ../memories/cloudmappr/strategy/svg-pre-render-experiment.md

- [ ] @accept prove conversion from upstream TopoJSON into standalone SVG path
      artifacts preserves land coverage, antimeridian behavior, and geometry order
- [ ] @accept prove pre-rendered land aligns with runtime D3-projected points and
      labels under the same projection/viewBox contract
- [ ] @accept compare artifact size, network cost, parse work, render work,
      export parity, and lifecycle complexity against the active baseline
- [ ] @accept retain the D3/TopoJSON baseline unless the experiment has a measured
      advantage without reducing server/client map-spec fidelity
